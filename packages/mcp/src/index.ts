// MCP adapter. Speaks MCP to the editor; forwards ListTools/CallTool to the
// tool-server over HTTP. Spawns the tool-server if it isn't running.
// TODO: implement the stdio MCP server + tool-server client.
export async function startMcp(): Promise<void> {
  // TODO
}
