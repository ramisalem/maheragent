{
  "name": "@ramisalem/mcp",
  "version": "0.1.0",
  "description": "MCP adapter that forwards tool calls to the tool-server",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch"
  }
}
