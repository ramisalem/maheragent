{
  "name": "@ramisalem/mcp",
  "version": "0.1.0",
  "description": "MCP adapter that forwards tool calls to the tool-server",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "bin": {
    "maheragent-mcp": "dist/bin.js"
  },
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "pretest": "tsc --build",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@ramisalem/tool-server": "0.1.0"
  }
}
