{
  "name": "@ramisalem/registry",
  "version": "0.1.0",
  "description": "URN-keyed registry: resolves a tool's service dependencies and runs it",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch"
  },
  "dependencies": {
    "zod": "^3.23.8"
  }
}
