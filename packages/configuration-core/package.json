{
  "name": "@ramisalem/configuration-core",
  "version": "0.1.0",
  "description": "Feature flags storage (global + project scope)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "pretest": "tsc --build",
    "test": "node --test test/*.test.mjs"
  }
}
