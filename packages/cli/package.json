{
  "name": "@ramisalem/cli",
  "version": "0.1.0",
  "description": "CLI subcommands (run, tools, server, enable, flags)",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "pretest": "tsc --build",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@ramisalem/configuration-core": "0.1.0",
    "@ramisalem/mcp": "0.1.0",
    "@ramisalem/tool-server": "0.1.0"
  }
}
