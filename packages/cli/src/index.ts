// CLI subcommands: run <tool>, tools [describe <name>], server start/status/stop,
// enable/disable <flag>, flags, telemetry status/enable/disable.
// TODO: implement command handlers (talk to the tool-server via its client).
export async function runCli(_argv: string[]): Promise<void> {
  // TODO
}
