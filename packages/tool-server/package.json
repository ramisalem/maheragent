{
  "name": "@ramisalem/tool-server",
  "version": "0.1.0",
  "description": "Long-running daemon: registry + BrowserSession + tools",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "pretest": "tsc --build",
    "test": "node --test test/*.test.mjs"
  },
  "dependencies": {
    "@ramisalem/registry": "0.1.0",
    "playwright": "^1.49.0",
    "zod": "^3.23.8"
  }
}
