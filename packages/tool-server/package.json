{
  "name": "@ramisalem/tool-server",
  "version": "0.1.0",
  "description": "Long-running daemon: registry + BrowserSession + tools",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch"
  }
}
