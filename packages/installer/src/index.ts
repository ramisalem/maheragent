// Workspace install. init/update: detect the editor, register the MCP server in its
// config, copy skills into the workspace. uninstall reverses it.
// TODO: implement editor detection + MCP registration + skill copy.
export async function runInstaller(_argv: string[]): Promise<void> {
  // TODO
}
